Sono possibili diversi comandi:£GetInstructions            per visualizzare le istruzioni relative ai comandi;GetMetadata                per visualizzare la corrispondenza tra variabili, significato e tipo;GetData                    per visualizzare tutti i dati immagazzinati nel database;GetData       (POST+body)  per visualizzare solo i dati che rispettono il filtro inserito nel body;GetStats                   per visualizzare tutte le statistiche elaborate su tutto il database;GetStats      (POST+body)  per visualizzare le statistiche su un campione del database che rispetta il filtro inserito;GetFieldStats              per visualizzare le statistiche solo sul campo imposto;GetFieldStats (POST+body)  per visualizzare le statistiche solo sul campo imposto su un campione del database che rispetta il filtro inserito;£Gli operatori che si possono usare nella forma 'filter={'campo':{'operatori':valori}}':£$in :[val,...]     per selezionare solo i post con almeno un valore tra quelli inseriti;$not:val           per selezionare solo i post senza nessun valore tra quelli inseriti;$nin:[val,...]     per selezionare solo i post senza almeno un valore tra quelli inseriti;$gt :val           per selezionare solo i post con campo numerico maggiore del valore           (>val);$gte:val           per selezionare solo i post con campo numerico maggiore o uguale del valore  (>=val);$lt :val           per selezionare solo i post con campo numerico minore del valore             (<val);$lte:val           per selezionare solo i post con campo numerico minore o uguale del valore    (<=val);$bt :[val1,val2]   per selezionare solo i post con campo numerico compreso nei valori           (val1< <val2);£Per poter usare più filtri insime si ha la possibilità di usare i macro-operatori 'filter={'macro-operatore':[{'campo':{'operatori':valori}},...]}':£$or    si prendono in considerazione solo i post che soddisfano almeno una delle condizioni;$and   si prendono in considerazione solo i post che soddisfano tutte le condizioni;£Per una descrizione completa della applicazione visitare la pagina:£https://github.com/valerio1805/ProgettoProgrammazione2k20